# ─────────────────────────────────────────────────────────────
# 派生资产：体积大、可由源头重新生成、不进版本库
# git 对二进制不做增量，进去一次就永久膨胀，且历史删不掉
# ─────────────────────────────────────────────────────────────
assets/
*.usd
*.usda
*.usdc
*.stl
*.STL
*.dae
*.obj

# 模型权重与导出产物
*.pt
*.pth
*.onnx
*.jit

# ─────────────────────────────────────────────────────────────
# 训练产物
# ─────────────────────────────────────────────────────────────
logs/
outputs/
runs/
wandb/
.hydra/

# ─────────────────────────────────────────────────────────────
# Python
# ─────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# ─────────────────────────────────────────────────────────────
# 编辑器 / 系统
# ─────────────────────────────────────────────────────────────
.idea/
.DS_Store
*.swp
.vscode/
# ─────────────────────────────────────────────────────────────
# 明确要跟踪的例外
# 接缝契约是「真相载体」，必须进版本库，出问题第一件事就是 git diff 它
# ─────────────────────────────────────────────────────────────
!docs/contracts/
!docs/contracts/*.json

# ── 例外：bundle 是交付物本身，不是训练中间产物 ──
# 小（合起来 330 KB）、一次性、明天板子上 clone 的就是它
!**/bundle/*.onnx
!**/bundle/*.pt
